@model IQueryable<Product>
@{
    ViewData["Title"] = "Sweatpants";
}

<div class="container">
    <h1 id="Sweatpants">BAD ART SWEATPANTS</h1>
    <div class="card-columns">
        @foreach (Product p in Model)
        {
            <partial name="_ProductPartialView" model="p" />
        }
    </div>
</div>

