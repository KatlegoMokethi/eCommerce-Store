@model Product

<div class="card">
    <div class="card-body">
        <img class="card-img" src="@Model.ImagePath" alt="@Model.Name">
        <div class="card-text">
            <h5 class="text-right">R @Model.Price.ToString()</h5>
            <h3>@Model.Name</h3>
            <p>@Model.Description</p>
            @if (Model.status == Status.Available)
            {
                <form id="@Model.ProductId" asp-action="AddToCart"
                      asp-controller="Cart" method="post">
                    <input type="hidden" asp-for="ProductId" />
                    <input type="hidden" name="returnUrl"
                           value="@ViewContext.HttpContext.Request.PathAndQuery()" />
                    <span class="card-text p-1">
                        @*@if (User.Identity.IsAuthenticated){*@
                        <button type="submit"
                                class="btn btn-success btn-sm pull-right float-right">
                            Add To Cart
                        </button>
                        @*}*@
                    </span>
                </form>
            }
            else
            {
                <span class="btn-danger p-1 mb-3 float-right">Out of stock</span>
            }
        </div>
    </div>
</div>